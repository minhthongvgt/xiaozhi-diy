# Xiaozhi Custom Hardware Project Kconfig
# Sources main component project build configuration

mainmenu "Xiaozhi Firmware Configuration"

# Load the main project menu configuration
source "main/Kconfig.projbuild"
